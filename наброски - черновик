# Домашнее задание, Условия задачи:
print("Задание 2: Дан текст: в первой строке задано число строк, далее идут сами строки.\nВыведите слово, которое в этом тексте "
      "встречается чаще всего.\nЕсли таких слов несколько, выведите то, которое меньше в лексикографическом порядке. "      "Слово которое первое повторилось.\n")
print("Чтобы вставить готовый текст, введите - 1\nЕсли желаете ввести текст вручную, введите - 2")
g1 = int(input("Введите 1 или 2 : "))
stroka_1 = ""  # Все введенные строки хранятся в одной строке
if g1 == 1:  # вставляем готовые строки
    N = 4
    a_1 = "Dog? stop! dog. caT."
    a_2 = "Cat, mat, mog. Cat smog?"
    a_3 = "top, gop, top top"
    a_4 = "Просто текст! Тут же может быть, просто текст?"
    stroka_1 = a_4 + "\n" + a_3 + "\n" + a_2 + "\n" + a_1 + "\n"
    print("\nНаша строка:\n", stroka_1)
else:  # пользователь сам выбирает сколько и каких строк будет
    N = int(input("Введите кол-во строк: "))
    stroka_1 = "" # Создаем пустую строку #[] # Создаем пустой список для рядов ( set1 = )
    print("\nНаша строка:\n")
    for cikl_1 in range(N):  # Создаем столько строк, сколько указал пользователь
        set1 = input(f"{cikl_1 + 1}. Введите строку:")  # Вписываем содержимое строки
        stroka_1 += set1 + "\n"  # сохраняем полученную строку
        print(cikl_1 + 1, set1)# Тк цикл начинается с 0, добавим +1 для удобного отображения порядкового номера
# ввели строки в переменную stroka_1
print("\nПроцесс выполнения задания по шагам:\n")# Все введенные ранее строки хранятся в одной строке в переменной stroka_1
print("Шаг_1: Предлагаем пользователю ввести сколько и каких строк будет:\n", stroka_1)  # Введенные строки# делаем буквы одного регистра и сохраняем в переменную stroka_2
stroka_2 = str(stroka_1).lower()  # Меняем все на строчный регистр
print("Шаг_2: Делаем все символы одного регистра:\n", stroka_2)  # Смотрим результат 2 шага# # stroka_3 = stroka_2.split("\n") # Разбиваем строку, для дальнейшей работы по индексам
## # Так может не нужно разбивать строку? у нас метка строк стоит, главное ее не потерять при чистке символов
# # для этого надо прописать символ деления строк к условию
#print("stroka_3: разбиваем слова на буквы, чтобы проверить на наличие др. символов")
stroka_3 = list()  # создаем пустой список, для сохранения слов без символов# stroka_4 =  запасной план list()
alfavit = "abcdefghijklmnopqrstuvwxyz"  # английский алфавит
rus_alfavit = "абвгдеёжзийклмнопрстуфхцчшщъыьэюя"  # русский алфавит
a1 = []  # создаем пустой список для уникальных слов
a2 = []  # создаем пустой список для повторяющихся слов
# Теперь нужно почистить строки от лишних символов
set3 = ""  # создаем пустую строку, для сохранения слов без символов
for cikl_2 in range(len(stroka_2)):  # создаем цикл для проверки букв и удаления ненужных символов

    # print(cikl_2 + 1, "проверяемый символ: ", stroka_2[cikl_2])
    for cikl_3 in rus_alfavit:  # перебираем буквы русского алфавита
        # print(" буква русская для сравнения: ", cikl_3)
        if stroka_2[cikl_2] == cikl_3:
            # print("\n-------rus-----------\n")
            set3 += stroka_2[cikl_2]
    for cikl_4 in alfavit: #перебираем буквы английского алфавита
        # print(" буква английская для сравнения: ", cikl_4)
        if stroka_2[cikl_2] == cikl_4:
            # print("\n-------angl-----------\n")
            set3 += stroka_2[cikl_2]
    if stroka_2[cikl_2] == " ":  # создаем условие: находим в нашем списке только русские и английские буквы
            # print("\n-------probel -----------\n")
            set3 += stroka_2[cikl_2]
    if stroka_2[cikl_2] == "\n":
            # print("\n-------enter -----------\n")
            # stroka_3.append(set3) пока не будем добавлять в список
            # print("______stroka_3_do split______\n", set3)
            s1 = set3.split()
            n1 = 1
            for cikl_5 in range(len(s1)):  # прогон букв по индексу из строки
                    a1.append(s1[cikl_5])  # добавляем поочередно элементы списка
                    k1 = a1.count(s1[cikl_5])  # создаем переменную для поиска дубляжа: 1-слово не повторяется, 2 и более-повторяется
                    # print("a1", a1)
                    if k1 == 1:
                            pass
                    elif k1 == 2:  # создаем условие, при нахождении дубляжа
                            a1.pop()
                            a2.append(cikl_5)  # перемещаем повторяющиеся слова в другой список
                            print(f"{n1}. Cлово \" {s1[cikl_5]} \" - Повторяется {s1.count(s1[cikl_5])} раз(а)")
            stroka_3.append(s1)
            # print("______stroka_3_posle split______\n", stroka_3)
            set3 = ""
            n1 += 1

            # проверим нашу строку на дубляж символов
            # for i7 in range(len(set(split_2))):
# осталось только чтобы выводилось 1 слово из списка при дубляже
# 
